@if (ViewBag.NoNavBar == null || !ViewBag.NoNavBar)
{
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>

                <a href="@Url.Action("Index", "Avaliacao", new { Area = "" })"> <img src="~/Content/logo-infnet.jpg" style="height:50px" alt="square logo" /></a>
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Avaliações", "Index", new { Controller = "Avaliacao", Area = "" })</li>
                    @if (User.IsInRole("admin"))
                    {
                        <li>@Html.ActionLink("Administrativo", "Index", new { Area = "Admin", Controller = "Dashboard" })</li>
                    }
                </ul>
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>
}
